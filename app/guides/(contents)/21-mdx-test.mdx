export const metadata = {
  title: 'MDX Test',
  description: '',
  labels: ['mdx']
};

<h1>Lorem ipsum</h1>
<h2>Dolor sit amet 😪</h2>
<h3>consectetur &amp; adipisicing</h3>
<h4>elit</h4>
<h5>elit</h5>

// Имя файла, выбор языка, кнопка скопировать

:::code-group

```ts fileName='ts-example.ts' language='ts'
type Message = string;

const message: Message = 'Hello from TypeScript';

console.log(message);
```

```js fileName='js-example.js' language='js'
const message = 'Hello from JavaScript';

console.log(message);
```

:::

// Имя файла, кнопка скопировать

```json fileName='package.json'
{
  "scripts": {
    "dev": "npm run dev",
    "build": "npm run build",
    "test": "npm run test",
    "lint": "npm run lint"
  }
}
```

// Кнопка скопировать

```json
{
  "scripts": {
    "dev": "npm run dev"
  }
}
```

// Имя файла

```json fileName='test.json'
{
  "scripts": {
    "dev": "npm run dev"
  }
}
```

// Plain text

```text
project-root/
├── package.json
├── package-lock.json  # Единственный lock-файл от npm
├── node_modules/
```
