export const metadata = {
  title: 'MDX Test',
  description: '',
  labels: ['mdx']
};

<h1>Lorem ipsum</h1>
<h2>Dolor sit amet 😪</h2>
<h3>consectetur &amp; adipisicing</h3>
<h4>elit</h4>
<h5>elit</h5>

// Имя файла, выбор языка

:::code-group

```ts [ts-example.ts]
type Message = string;

const message: Message = 'Hello from TypeScript';

console.log(message);
```

```js [js-example.js]
const message = 'Hello from JavaScript';

console.log(message);
```

:::

// Имя файла

```json [package.json]
{
  "scripts": {
    "dev": "npm run dev", // [!code --]
    "build": "npm run build", // [!code ++]
    "test": "npm run test",
    "lint": "npm run lint"
  }
}
```

// Plain text

```text
project-root/
├── package.json
├── package-lock.json  # Единственный lock-файл от npm
├── node_modules/
```
