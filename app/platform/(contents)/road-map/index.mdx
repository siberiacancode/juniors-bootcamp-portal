import { Link, Meta } from '@/components/ui';

import { Mention } from '@/app/platform/[...slug]/components';

export const metadata = {
  emoji: '🗺️',
  title: 'Road map для новичков'
};

Привет, данная страница создана для быстрого экскурса новичков в **JUNIORS BOOTCAMP**
